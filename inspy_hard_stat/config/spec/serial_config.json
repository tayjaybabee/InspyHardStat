{
    "serial_port": {
        "type": "string",
        "default": "COM11",
        "description": "The serial port to use for communication with the device."
    },

    "baud_rate": {
        "type": "int",
        "default": "115200",
        "description": "The baud rate to use for communication with the device."
    },

    "end_sequence": {
        "type": "string",
        "default": "\n\n",
        "description": "The sequence of characters that indicates the end of a message from the device."
    }
}
